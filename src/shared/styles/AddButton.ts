import styled from "styled-components";

export const AddButton = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  color: #888;
  font-size: 24px;
  padding: 0;
  margin-left: auto;
  display: flex;
  align-items: center;

  &:hover {
    color: #555;
  }
`;
